kind: Deployment
apiVersion: apps/v1beta1
metadata:
  name: ita-power-bi-gateway
  namespace: mdsnamespace
  labels:
    app: ita-power-bi-gateway
spec:
  replicas: 1
  strategy:
    rollingUpdate: 
      maxSurge: 1 
      maxUnavailable: 1 
  minReadySeconds: 5  
  template:
    metadata:
      labels:
        app: ita-power-bi-gateway
    spec:
      containers:
      - name: ita-power-bi-gateway
        image: dataservices.azurecr.io/ita-power-bi-gateway:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        env:
          - name: TENANT_ID
            value: <Azure Tenant ID>
          - name: CLIENT_ID
            value: <Application ID from the AD App Registration>
          - name: CLIENT_SECRET
            value: <Client secret from the AD App Registration>
          - name: WORKSPACE_ID
            value: <Power BI Workspace ID / Group>
      restartPolicy: Always
---

kind: Service
apiVersion: v1
metadata:
  name: ita-power-bi-gateway
  namespace: mdsnamespace
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
  selector:
    app: ita-power-bi-gateway
---

apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: ita-power-bi-gateway
  namespace: mdsnamespace
spec:
  rules:
    - host: <ip-dns-name>.<location>.cloudapp.azure.com
      http:
        paths:
          - path: /
            backend:
              serviceName: ita-power-bi-gateway
              servicePort: 80
  tls:
    - hosts:
        - <ip-dns-name>.<location>.cloudapp.azure.com
      secretName: tls-secret